version: "3"

services:
  three-lods:
    build:
      context: ./
      dockerfile: ./Dockerfile-dev
    container_name: three-lod-manager
    ports:
      - "5173:5173"
    volumes:
      - ./src/:/app/src/
      - ./public/:/app/public/
    networks:
      - default